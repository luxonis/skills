# Narrow existing-app edit

The existing DepthAI v3 application has a passing detection pipeline, representative evidence,
and an approved plan. Add the existing track ID and detection confidence to its annotated output.
Do not change sensors, model, preprocessing, topology, or acceptance behavior.

Explain whether a new plan review is required and perform only the scoped edit.
