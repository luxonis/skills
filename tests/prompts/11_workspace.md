# Workspace bootstrap case

You are in a project folder with no `AGENTS.md`. oakctl may be missing. The user says:

> Make this folder a Luxonis workspace so later chats have AGENTS.md, oakctl, and a project env that can import DepthAI.

Use `luxonis-workspace`. Explore first. Ask before sudo. Do not discover cameras, do not prove
a stream, do not build an app, do not ask which camera to buy. Seed `docs/glossary.md` only if
missing. Do not write `docs/device.md`. If `CLAUDE.md` exists, add `@AGENTS.md` without
overwriting user content.
