# Sparse new-app case

You are in an empty project directory. The user says:

> I have an OAK 4. Build an app that scans barcodes on boxes going by and sends them to my system.

Use the installed Luxonis skills as they would normally be available. Respond and act exactly as
you would for the customer. Do not invent the business problem. Obtain `PROJECT_BRIEF.md` from
the user (or draft it from stated facts for confirmation), write `POC_PLAN.md`, then use
`luxonis-record` to capture a holistic recording before iterating.
