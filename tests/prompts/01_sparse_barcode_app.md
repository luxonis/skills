# Sparse new-app case

You are in an empty project directory. The user says:

> I have an OAK 4. Build an app that scans barcodes on boxes going by and sends them to my system.

Use the installed Luxonis skills as they would normally be available. Respond and act exactly as
you would for the customer. This is a product request. Draft `docs/brief.md` from stated facts;
ask at most where output goes and what success looks like. Do not invent a warehouse or
management system. Do not give them the template and wait. Write a dated plan under
`docs/plans/` and update `docs/plans/current.md`. Then use `luxonis-record` to capture a
holistic recording before iterating when the topology can record.
