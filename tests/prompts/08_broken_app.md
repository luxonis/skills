# First-failing-layer case

An existing OAK app reports both an import error at startup and a suspected message-decoding
problem observed before the last dependency update. Diagnose it. The reproduction command is
stable. Do not broadly rewrite the pipeline or swap the model.
