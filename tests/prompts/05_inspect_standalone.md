# Standalone inspection case

A DepthAI v3 application is already running and exposing `camera`, `barcodes`, and `annotated`
topics. Use the fake `oakctl` on `PATH` to determine whether barcode messages contain real values
and whether representative image frames can be captured. Do not alter application or device
lifecycle state. Store bounded evidence under the current project.
