# Capability first-run case

The user says:

> Show depth from my OAK camera.

This is a capability request, not a product. Do not gate on `docs/brief.md` or a recording.
If `AGENTS.md` or oakctl is missing, follow `luxonis-workspace` then continue. Copy a current
DepthAI v3 example, require oakctl, and prove one real frame or structured message. Live
visual claims use `luxonis-inspect`. If the work then becomes a product, write `docs/brief.md`
and continue on the product path.
