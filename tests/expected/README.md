# Expected behavior

This directory documents the stable grading contract without storing one preferred prose answer.
Use `TEST_MATRIX.md` for objective graders and `activation_cases.json` for routing labels.

Behavioral outputs may vary in wording. Grade artifacts, commands, terminal states, evidence, and
scope decisions instead of exact text.
